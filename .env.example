username=your_instagram_username
email=your_email@gmail.com
password=your_password

# Default login method for authenticate.py: facebook, email, username, manual
LOGIN_METHOD=facebook

# Optional settings
TOTP_SEED=
VERIFICATION_CODE=
PHONE_NUMBER=
# IPRoyal/ISP proxy in host:port:username:password format
PROXY=host:port:username:password
CHROME_PATH=
CHROME_DEBUG_PORT=9222
# Use your real Chrome profile (default: a dedicated chrome_profile/ folder in this repo)
USE_MY_CHROME_PROFILE=false

# Session files (stored in data/ folder by default)
# Leave BROWSER_STATE_FILE/BROWSER_COOKIES_FILE unset in multi-user (dashboard/
# server) deployments — server/jobs.py sets BROWSER_STATE_FILE per-user on the
# bot subprocess env, and a static value here would override that and break
# per-user session isolation. Only set these for a single-user local run.
SESSION_FILE=data/session.json

HASHTAG_TO_SEARCH=realestate
COMMENT_TEXT=Incredible work! Thanks for sharing this.
DM_MESSAGE=Hey there! I saw your post under the hashtag and wanted to connect.
MAX_COMMENTS=1
MAX_DMS=0

# Run browser bot instead of instagrapi API
USE_BROWSER=true

# Convex database (run `npx convex dev` to get this URL, then paste it here)
CONVEX_URL=https://your-deployment.convex.cloud

# Gemini agent (add key before running agent — not needed for tool tests)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SESSION_MINUTES=20
MAX_COMMENTS_PER_SESSION=2
MAX_REPLIES_PER_SESSION=3
MAX_LIKES_PER_SESSION=12
MAX_FOLLOWS_PER_SESSION=5
MAX_DMS_PER_SESSION=2
AGENT_PERSONA=You are a friendly developer who enjoys Python and coding content on Instagram.
AGENT_DRY_RUN=false
